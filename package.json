{
  "name": "rebuilt-workers",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "update-d1-schema-local": "wrangler d1 execute rebuilt_db --local --file=./schema.sql",
    "update-d1-schema-remote": "wrangler d1 execute rebuilt_db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.20",
    "chanfana": "^3.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/service-worker-mock": "^2.0.4",
    "wrangler": "^4.69.0"
  }
}
